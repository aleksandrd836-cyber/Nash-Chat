# Зависимости — устанавливаются командой npm install
node_modules/

# Собранные файлы Electron — создаются командой npm run electron:build
dist-electron/

# Собранный сайт — создаётся командой npm run build
dist/

# Переменные окружения (ключи Supabase — секретные!)
.env
.env.local
.env.production

# Системные файлы
.DS_Store
Thumbs.db

# Логи
*.log
npm-debug.log*
